spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/catalog_configuration_test
    username: kanokna_test
    password: kanokna_test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false

logging:
  level:
    com.kanokna.catalog: DEBUG
